# การกรองข้อมูลเว็บไซต์ 404 และโดเมนหมดอายุ

## ภาพรวม

สคริปต์ `filter_404_expired.py` ช่วยกรองข้อมูลจากไฟล์ CSV ที่ export แล้ว ให้เหลือเฉพาะเว็บไซต์ที่มีปัญหาจริงๆ:

- **HTTP_ERROR_4XX** - รวม 404 Not Found errors
- **NO_DNS** - โดเมนหมดอายุ/ไม่พบ DNS record
- **DEAD_DOMAIN** - โดเมนตาย
- **SSL_ERROR** - SSL certificate มีปัญหา (อาจเป็นโดเมนมีปัญหา)

## วิธีใช้งาน

### 1. กรองไฟล์ที่มีอยู่

```bash
# กรองไฟล์และสร้างไฟล์ใหม่ (จะเพิ่ม _404_expired)
python filter_404_expired.py output/dead_websites_Chiang_Mai_20251210_133345.csv

# ระบุชื่อไฟล์ output เอง
python filter_404_expired.py output/dead_websites_Chiang_Mai_20251210_133345.csv filtered_leads.csv
```

### 2. ตัวอย่างผลลัพธ์

```
📊 อ่านข้อมูลได้ 45 รายการ

📈 สถิติเริ่มต้น:
   HTTP_ERROR_4XX: 12 รายการ
   NO_DNS: 8 รายการ
   SSL_ERROR: 5 รายการ
   TIMEOUT: 15 รายการ
   OK: 5 รายการ

✅ บันทึกไฟล์สำเร็จ: output/dead_websites_Chiang_Mai_20251210_133345_404_expired.csv
📊 จำนวนรายการที่กรองแล้ว: 25 จาก 45 รายการ

📈 สถิติหลังการกรอง:
   HTTP_ERROR_4XX: 12 รายการ
   NO_DNS: 8 รายการ
   SSL_ERROR: 5 รายการ

🎯 ตัวอย่างเว็บไซต์ที่กรองได้:
   1. ร้านอาหารสมชาย - https://somchai-restaurant.com (HTTP_ERROR_4XX)
   2. คลินิกหมอสุดา - https://expired-domain.com (NO_DNS)
   3. อู่ซ่อมรถวิชัย - https://ssl-problem.com (SSL_ERROR)
```

## สถานะที่กรอง vs ที่ไม่กรอง

### ✅ กรองเอาไว้ (เป็น Lead ที่ดี):
- **HTTP_ERROR_4XX** - 404 Not Found, 403 Forbidden, etc.
- **NO_DNS** - โดเมนหมดอายุ/ไม่พบ DNS
- **DEAD_DOMAIN** - โดเมนตาย
- **SSL_ERROR** - SSL certificate หมดอายุหรือมีปัญหา

### ❌ ไม่กรอง (ไม่น่าสนใจ):
- **TIMEOUT** - อาจเป็นเซิร์ฟเวอร์ช้า ไม่ใช่ปัญหาจริง
- **CONNECTION_ERROR** - อาจเป็นปัญหาชั่วคราว
- **REDIRECT_PARKING** - โดเมนถูก park แต่ยังใช้งานได้
- **UNDER_CONSTRUCTION** - เว็บกำลังพัฒนา ไม่ได้ตาย

## โครงสร้างไฟล์ Output

ไฟล์ที่กรองแล้วจะมี columns เหมือนเดิม แต่มีเฉพาะแถวที่ตรงเงื่อนไข:

- `business_name` - ชื่อธุรกิจ
- `business_category` - หมวดหมู่ธุรกิจ
- `phone` - เบอร์โทรศัพท์
- `website_url` - URL เว็บไซต์
- `website_status` - สถานะเว็บไซต์ (จะเป็น 4 สถานะที่กรองแล้ว)
- `status_reason` - เหตุผลที่เว็บไซต์มีปัญหา
- `address` - ที่อยู่
- `rating` - คะแนนรีวิว
- `user_ratings_total` - จำนวนรีวิว
- `place_id` - Google Place ID

## เคล็ดลับการใช้งาน

1. **เรียงลำดับ**: ข้อมูลจะเรียงตาม `website_status` และ `rating` (คะแนนสูง → ต่ำ)
2. **คุณภาพ Lead**: ธุรกิจที่มี rating สูงและรีวิวเยอะ มักเป็น Lead ที่ดีกว่า
3. **ติดต่อ**: ใช้เบอร์โทรใน column `phone` สำหรับติดต่อ
4. **ตรวจสอบเพิ่มเติม**: ควรเข้าไปดู website จริงก่อนติดต่อเพื่อยืนยันว่ามีปัญหาจริง
